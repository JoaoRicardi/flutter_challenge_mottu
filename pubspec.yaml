name: flutter_challenge
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  #di
  get_it: ^7.2.0

  #firebase
  firebase: ^9.0.1
  firebase_core: ^1.18.0

  #handle svg assets
  flutter_svg: ^0.23.0+1

  #statemanagment
  mobx: ^2.0.7
  flutter_mobx: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  build_runner: ^2.2.0

  # gerar statemanagment
  mobx_codegen: ^2.0.6


flutter:
  uses-material-design: true

  assets:
    - assets/images/
